The Predictor folder contains the XCode workspace and the Python folder contains the Python script. To run, first run the
python script which will start the Flask server. Then, run the app on XCode. 
